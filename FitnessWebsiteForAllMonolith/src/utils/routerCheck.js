/**
 * This file exists solely to ensure react-router-dom is resolvable at build time.
 * It exports BrowserRouter so that the bundler loads the package during build.
 */
export { BrowserRouter } from "react-router-dom";
